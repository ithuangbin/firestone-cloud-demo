# eureka 主机/端口配置 start
prod1:
  eureka:
    server:
      port: 8501
    hostname: localhost
prod2:
  eureka:
    server:
      port: 8502
    hostname: localhost
dev:
  eureka:
    server:
      port: 8500
    hostname: localhost
# eureka 主机/端口配置 end

spring:
  application:
    name: firestone-zipkin
  profiles:
    active: dev
  boot:
    admin:
      client:
        url: http://localhost:8515

server:
  port: 8600

management:
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    health:
      show-details: ALWAYS